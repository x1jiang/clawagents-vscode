# ClawAgents sidecar dependencies (auto-installed into clawagents.pythonPath)
#
# On first start / Restart Sidecar, the extension runs:
#   python -m pip install -U 'clawagents[gemini,anthropic,bedrock,mcp]>=6.13.0,<7' 'fastapi>=0.115,<1' 'uvicorn>=0.30,<1' 'pydantic>=2.7,<3' 'python-dotenv>=1,<2'
#
# Or Command Palette → ClawAgents: Install/Upgrade Python Dependencies
# Optional ATLAS: pip install 'atlas-skill @ git+https://github.com/multi-agent-systems-failure-taxonomy/ATLAS.git'
#
clawagents>=6.13.0,<7
fastapi>=0.115.0,<1
uvicorn>=0.30.0,<1
pydantic>=2.7.0,<3
python-dotenv>=1.0.0,<2
