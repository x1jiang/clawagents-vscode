# ClawAgents sidecar dependencies (auto-installed into clawagents.pythonPath)
#
# On first start / Restart Sidecar, the extension runs:
#   python -m pip install -U 'clawagents[gemini,anthropic,bedrock,mcp]>=6.13.1,<7' ...
#   plus the pinned atlas-skill Git revision (built-in ATLAS, Settings default on).
#
# Or Command Palette → ClawAgents: Install/Upgrade Python Dependencies
#
clawagents>=6.13.1,<7
fastapi>=0.115.0,<1
uvicorn>=0.30.0,<1
pydantic>=2.7.0,<3
python-dotenv>=1.0.0,<2
atlas-skill @ git+https://github.com/multi-agent-systems-failure-taxonomy/ATLAS.git@3a917f3e0b993e3bfd77f652b013193aed167964
