# ClawAgents sidecar dependencies (auto-installed into clawagents.pythonPath)
#
# On first start / Restart Sidecar, the extension runs:
#   python -m pip install -U 'clawagents[gemini,anthropic,bedrock,mcp,media,accurate-tokens,pty]>=6.20.42,<7' ...
#
clawagents[gemini,anthropic,bedrock,mcp,media,accurate-tokens,pty]>=6.20.42,<7
tiktoken>=0.5.0,<1
fastapi>=0.115.0,<1
uvicorn>=0.30.0,<1
pydantic>=2.7.0,<3
python-dotenv>=1.0.0,<2
